<%= form_for(@table3) do |f| %>
  <% if @table3.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@table3.errors.count, "error") %> prohibited this table3 from being saved:</h2>

      <ul>
      <% @table3.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :code %><br>
    <%= f.number_field :code %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
